{
    "buildCommand": "cd HowManyTimeLeft/frontend && npm install && npm run build",
    "outputDirectory": "HowManyTimeLeft/frontend/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}
